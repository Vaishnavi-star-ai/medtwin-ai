{
  "name": "medtwin-ai",
  "version": "3.0.0",
  "description": "MedTwin AI — Smart Healthcare Platform",
  "scripts": {
    "install:all": "cd server && npm install && cd ../client && npm install",
    "build:client": "cd client && npx vite build --outDir ../server/public",
    "start": "cd server && node server.js",
    "dev": "cd server && node server.js",
    "render-build": "npm run install:all && npm run build:client"
  }
}
